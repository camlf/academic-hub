apiVersion: v1
kind: Service
metadata:
  name: nbviewer 
spec:
  ports:
  - port: 8080 
  selector:
    app: nbviewer 
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nbviewer 
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  selector:
    matchLabels:
      app: nbviewer
  template:
    metadata:
      labels:
        app: nbviewer
    spec:
      containers:
      - name: nbviewer 
        image: academicpi.azurecr.io/nbviewer:v20
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 250m

